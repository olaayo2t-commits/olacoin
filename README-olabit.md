# Olabit contract

This repository includes a Clarity fungible token contract for Olabit.

- Contract: `contracts/olabit.clar`
- Manifest: `Clarinet.toml`

Install Clarinet (via npm):

```bash
npm i -g @hirosystems/clarinet
```

Check the project:

```bash
clarinet check
```
